{
  "name": "lepaya-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.0.6"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.15",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
